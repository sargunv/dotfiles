starship:
  preset: nerd-font-symbols
  config:
    format: "$all"
    right_format: "$time"
    command_timeout: 1000
    git_metrics:
      disabled: false
    os:
      disabled: false
    sudo:
      disabled: false
    time:
      disabled: false
      style: dimmed
    status:
      disabled: false
      symbol: "✘"
