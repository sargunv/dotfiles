starship:
  preset: nerd-font-symbols
  config:
    format: "$all"
    right_format: "$os$custom$shell$time"
    git_metrics:
      disabled: false
    os:
      disabled: false
    shell:
      disabled: false
    time:
      disabled: false
    custom:
      arch:
        when: "true"
        command: "arch"
        style: ""
