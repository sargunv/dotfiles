starship:
  preset: nerd-font-symbols
  config:
    format: "$all"
    right_format: "$os$custom$shell$time"
    command_timeout: 1000
    git_metrics:
      disabled: false
    os:
      disabled: false
    shell:
      disabled: false
    sudo:
      disabled: false
    time:
      disabled: false
    status:
      disabled: false
      symbol: "✘"
    custom:
      arch:
        when: "true"
        command: "arch"
        style: ""
