#!/bin/bash

# hash: {{ include (expandenv "$HOME/Library/LaunchAgents/com.github.sargunv.dotfiles.update.plist") | sha256sum }}

plist_path="$HOME/Library/LaunchAgents/com.github.sargunv.dotfiles.update.plist"
launchctl unload "$plist_path"
launchctl load "$plist_path"
